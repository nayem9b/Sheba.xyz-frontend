"use client";

import React from "react";

const EditServicepage = ({ params }: { params: any }) => {
  const { id } = params;

  return (
    <div>
      <h1>This is edit service page</h1>
    </div>
  );
};

export default EditServicepage;
